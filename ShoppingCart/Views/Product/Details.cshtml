@using Microsoft.AspNetCore.Identity
@using Persistence;
@inject SignInManager<IdentityUser> SignInManager
@inject UserManager<IdentityUser> UserManager
@model Domain.Models.Product

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Product</h4>
    <hr />
    <div class="row">
        <div class=" col-6">
            <img src="~/ProductImages/@Model.ProductImgPath" />
        </div>
        <div class=" col-6">
            <div class="row">
                <div class="col-4">
                    @*@Html.DisplayNameFor(model => model.NameEn)*@
                    <span style="font-size: large; font-weight:bold"> Product Name </span>
                </div>
                <div class="col-8">
                    @Html.DisplayFor(model => model.NameEn)
                </div>
            </div>
            <div class="row"></div>
            <div class="row">
                <div class="col-4">
                    @*@Html.DisplayNameFor(model => model.Price)*@
                    <span style="font-size: large; font-weight:bold"> Product Price </span>
                </div>
                <div class="col-8">
                    @Html.DisplayFor(model => model.Price) EGP
                </div>
            </div>
            <div class="row"></div>
            <div class="row"></div>
            <div class="row">
                <div class="col-4">
                    @*@Html.DisplayNameFor(model => model.Description)*@
                    <span style="font-size: large; font-weight:bold"> Description </span>
                </div>
                <div class="col-8">
                    @Html.DisplayFor(model => model.Description)
                </div>
            </div>
        </div>
        </div>
    <div>
        @if (User.IsInRole("Admin"))
        {
            <a asp-action="Edit" asp-route-id="@Model.Id">Edit</a> 
         }

        <a asp-action="Index">Back to List</a>
    </div>
</div>


@*@Html.DisplayFor(model => model.Category.NameEn)*@